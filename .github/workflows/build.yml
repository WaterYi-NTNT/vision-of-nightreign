name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v2
    
    - name: Set up MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Configure CMake
      run: cmake -B build -A x64
    
    - name: Build
      run: cmake --build build --config Release
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: VisionOfNightreign-v1.0.3-build-${{ github.run_number }}
        path: build/bin/Release/*.dll
        if-no-files-found: error

    - name: Create Release Zip
      if: startsWith(github.ref, 'refs/tags/')
      shell: pwsh
      run: |
        Compress-Archive -Path "build/bin/Release/*" -DestinationPath "VisionOfNightreign-${{ github.ref_name }}.zip"

    - name: Auto Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: VisionOfNightreign-v1.0.3.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}